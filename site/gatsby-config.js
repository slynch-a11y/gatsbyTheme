module.exports = {
  plugins: [
      {
          resolve: "gatsby-theme-a11y",
          options: {
              markdownPath: "src/markdown",
              basePath: "/hello"
          }
      }
  ]
}
